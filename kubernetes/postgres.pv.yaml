apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres
spec:
  storageClassName: postgres-storage
  capacity:
    storage: 4Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /pvs/postgres_db_storage
